<Window x:Class="Publisher.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Publisher.Views"
        mc:Ignorable="d"
        Title="MainView" Height="300" Width="300">

    <Border Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock>
                <Run Text="Версия:"/>
                <Run Text="{Binding Version}"/>
            </TextBlock>

            <TextBlock Grid.Row="1" TextTrimming="CharacterEllipsis">
                <Run Text="Ссылка на лаунчер:"/>
                <TextBox Text="LauncherLink" BorderThickness="0"/>
            </TextBlock>
        </Grid>

    </Border>

</Window>
